# Weather App

## Because everyone needs his own weather app

Everyone needs to know the weather, right? This app is a simple way to get the current weather data for a city. The app uses the OpenWeatherMap API to get the weather data for a given city.